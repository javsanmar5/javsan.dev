---
import { getCollection } from 'astro:content';
import ProjectsComponent from './tsx/Projects.tsx';

const projects = await getCollection('projects');
---

<ProjectsComponent client:load projects={projects} />
